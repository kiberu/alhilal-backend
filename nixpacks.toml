[variables]
NIXPACKS_PYTHON_VERSION = '3.11'

[phases.setup]
nixPkgs = ['python311', 'postgresql']

[phases.build]
# Skip collectstatic during build - will run at startup when DB is available
cmds = []

